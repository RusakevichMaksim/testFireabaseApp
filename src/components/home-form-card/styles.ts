import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  wrapper: {
    display: "flex",
    flexDirection: "column",
    maxWidth: "200px",
  },
  button: {
    marginTop: "10px",
  },
});
